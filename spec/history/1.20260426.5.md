# 1.20260426.5

## macOS: `macos doctor` のブリッジ時メッセージ整理

- **ブリッジ利用時**: `eventkit-helper` のパスが取れても「この doctor ではソケット経由のブリッジのみ使用し、カレンダー許可は .app 側」と明示（誤解防止）。
- **`calendarAccess: denied` + `transport: bridge`**: ヒントを **IDE ターミナル限定にせず常に表示**。ビルド／.app 差し替え直後の Finder での再オープンと、システム設定でのスイッチの切り替えを追記。
