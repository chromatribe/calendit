# カレンダー操作CLIツール `calendit` 仕様書

## バージョン管理
- **現在のバージョン**: `2026-0412-01.01` (Beta)
- **更新履歴**:
  - `2026-0412-01.01`: 初版作成（基本設計、Google/Outlook対応、CSV/MD/JSON入出力、バルク操作、コンテキスト設定）

---

## 1. 概要
`calendit` は、ターミナルから Google カレンダーおよび Outlook カレンダーの予定を照会・登録・操作するためのコマンドラインツールです。
特に、**LLM（AIエージェント）による自動操作**と、**人間によるドキュメントベースの管理**の両立を目的としています。

## 2. 主要機能
### 2.1 認証 (Auth)
- **方式**: 各サービスが提供する標準的な OAuth 2.0 Web Flow。
- **体験**: コマンド実行時にブラウザが開き、ユーザーがログイン・許可を行うと、ローカルの `calendit` がトークンを取得・保存します。
- **保存**: 認証トークンは OS の安全な場所に保存され、期限切れ時は自動的にリフレッシュされます。

### 2.2 カレンダー・予定操作 (CRUD)
- 個別の予定の照会、追加、編集、削除。
- カレンダー自体のリスト表示。

### 2.3 バルク操作 (Bulk Operations) ※重要
- **一括登録**: ファイル（CSV/MD/JSON）に記載された複数の予定を一度に登録。
- **一括リスケジュール**: 既存の予定を含むリストを読み込み、ID をキーにして一括で更新。

### 2.4 多彩な入出力フォーマット
- **CSV**: 表形式で一覧性が高く、スプレッドシートや AI のデータ処理に適しています。
- **Markdown (MD)**: 人間の視認性が高く、メモ帳感覚で予定を編集できます。
- **JSON**: AI ツールがプログラム的に処理するのに最適です。

---

## 3. データ仕様
各サービス（Google/Outlook）の差分を吸収し、共通のフォーマットで扱います。

### 3.1 共通イベントモデル
| フィールド名 | 説明 | 備考 |
| :--- | :--- | :--- |
| `id` | カレンダーサービス側で発行される一意のID | 編集・削除時に必須 |
| `summary` | 予定のタイトル | |
| `start` | 開始日時 (ISO 8601形式) | 例: `2024-04-12T10:00:00+09:00` |
| `end` | 終了日時 (ISO 8601形式) | |
| `location` | 場所 | |
| `description`| 説明・メモ | |
| `service` | `google` または `outlook` | |
| `calendar_id`| 対象カレンダーのID | |

### 3.2 IDの扱い
- **MD出力時**: 人間に邪魔にならないよう、HTMLコメント形式などで隠し項目として保持、または末尾に記載します。
  - 例: `[ ] **ミーティング** (10:00-11:00) <!-- id: g_abc123 -->`
- **CSV/JSON出力時**: 明示的な列/フィールドとして保持します。

---

## 4. コンテキスト設定 (Contexts)
用途（仕事、趣味など）に応じた設定をプリセットとして保持できます。

- **設定ファイル**: `~/.config/calendit/contexts.json`
- **内容**: どのサービスの、どのカレンダーIDを対象にするか、デフォルトのフォーマットは何か、等を定義。
- **利用**: コマンド実行時に `--set-work` や `--set-hobby` オプションで使用。

---

## 5. CLI コマンド体系 (案)

### 基本
- `calendit auth login <google|outlook>`
- `calendit config --name work --calendar <id>`

### 照会・出力
- `calendit query --set work --out-md schedule.md`
- `calendit query --start "2024-04-12" --end "2024-04-18" --format csv`

### 入力・更新 (バルク対応)
- `calendit apply --in-md schedule.md`
  - ファイル内の `id` を見て、存在すれば更新、なければ新規作成します。
- `calendit add --summary "ランチ" --start "12:00"`

---

## 6. 対応環境
- **OS**: macOS (Intel/Apple Silicon)
- **ランタイム**: Node.js v18以上
