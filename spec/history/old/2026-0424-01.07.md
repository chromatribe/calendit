# 2026-0424-01.07

## `calendit macos bridge build`

- **CLI**: `src/commands/macos.ts` — `macos bridge build` が `/bin/bash` 経由で `native/eventkit-bridge/scripts/build-app-bundle.sh` を実行（stdio 継承、macOS のみ）。
- **解決**: `src/core/macosBridgeApp.ts` — `resolveEventkitBridgeRepoPath()`：インストール済パッケージ隣の `native/eventkit-bridge`、または `CALENDIT_EVENTKIT_BRIDGE_ROOT`。npm 公開 tarball には `native` が入らないため、フルリポ或いは env 上書き時のみ利用可能。
- **ロケール** / **ドキュメント**: `macos.bridge.build*`、`commands.md`、`native/eventkit-bridge/README.md`、**for-ai-agents**（`CALENDIT_EVENTKIT_BRIDGE_ROOT`）。
