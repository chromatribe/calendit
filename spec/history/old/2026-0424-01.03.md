# 変更履歴 `2026-0424-01.03`

## EventKit ブリッジ — `.app` バンドルと LaunchAgent

- **`native/eventkit-bridge/AppBundle/`**: `Info.plist`（`NSCalendarsFullAccessUsageDescription` 等）、`PkgInfo`。
- **`native/eventkit-bridge/scripts/build-app-bundle.sh`**: `CalenditEventKitBridge.app` を `.build/` に生成し ad-hoc `codesign`。
- **`scripts/install-launchagent.sh` / `uninstall-launchagent.sh`**: `com.chromatribe.calendit.eventkit-bridge` を `~/Library/LaunchAgents` に配置し `launchctl bootstrap` / `bootout`。
- **`Sources/main.swift`**: Finder / `.app` 起動時（`-psn_*` のみ）でも `serve` とみなす。
