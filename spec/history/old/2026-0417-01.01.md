# 変更履歴 `2026-0417-01.01`

## 新機能

- **`calendit auth status`**: 全コンテキストの認証・トークン状態を表形式で表示。
- **設定ディレクトリの統一**: `src/core/auth.ts` のトークン・MSAL キャッシュパスを `CALENDIT_CONFIG_DIR` および `getCalenditConfigDir()` と一致させた（従来は `~/.config/calendit` に固定されていた）。

## その他

- ユーザー向け表出力用に `writeStdoutLine`（ログプレフィックスなし）を `src/core/logger.ts` に追加。
