# 変更履歴 `2026-0417-01.02`

## テスト

- **`docs/tests.md`**: `auth status` / Google トークン判定の回帰用に `TC-AUTH-02`〜`TC-AUTH-04` を追加。
  - `expiry_date` 未来 → `OK`
  - 期限切れ・リフレッシュなし → `EXPIRED`
  - 期限切れ・`refresh_token` あり → `OK`（コンテキスト名で行の存在を確認）

テストランナーが注入する `CALENDIT_CONFIG_DIR` 上に `printf` で `google_token_<context>.json` を生成して検証する。
