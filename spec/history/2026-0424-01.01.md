# 2026-0424-01.01

## テストマップ・診断ログ

- `docs/tests.md`: §2.6（`TC-I18N-*`）、§2.1 補足（`TC-AUTH-05`）、§2.7（`TC-ERR-META-01`）のマップと手順を追加・整理。
- `src/index.ts`: 失敗時に `logger.debug("ErrorMeta", JSON.stringify(...))` で種別・補助情報を 1 行に集約（`ValidationError` / `ConfigError` / `AuthError` / `ApiError` / `CalendarError` / unknown）。
- `src/test_runner.ts`: `DEBUG=calendit` 等の環境変数値に含まれる `calendit` を誤置換しないよう、CLI としての `calendit` のみを `node dist/index.js` に差し替える。
- `spec/spec.md` §2.7: ユーザー向け文言と `--verbose` / `DEBUG=calendit` / `--debug-dump` の役割を明文化。Outlook の `NOT CONFIGURED` 条件を §2.1 に追記。
