# Version 2026-0412-01.17

## 変更内容
- **CLI実行エラーの修正**:
  - `bin/cli.js` において、`ts-node/esm` ローダーのパスを絶対パスで解決するように修正。
  - これにより、`calendit` コマンドがプロジェクト外（グローバルパス経由等）から実行された場合でも、正しく `ts-node` を読み込み、`src/index.ts` を実行できるようになりました。

## 修正詳細
- `spawn` 引数の `--loader` に渡す値を、`path.resolve(__dirname, '../node_modules/ts-node/esm.mjs')` を使用して解決するように変更。

## 検証結果
- `node ./bin/cli.js --help` による基本動作の確認。
