# 2026-0423-01.01

## UI ローカライズ基盤

- `src/locales/en.json` / `ja.json` と `src/core/i18n.ts`（`t()`、`LocaleKey` 型は `scripts/gen-locale-keys.mjs` が生成）。
- `CALENDIT_LOCALE`、`--locale`、`config set-locale`、初回のみ言語選択（config 不在時）。
- `ConfigError.causeCode` で設定ファイル欠落を判定し、`loadConfigIfExists` と連携。
- `scripts/check-locale-parity.mjs`、`npm run check:locales`、GitHub Actions CI、`CONTRIBUTING.md`。
- テストは `dist/index.js` を実行し、`CALENDIT_LOCALE=en` / `CALENDIT_SKIP_LOCALE_PROMPT=1` を付与。
