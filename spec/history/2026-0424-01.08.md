# 2026-0424-01.08

## `calendit macos bridge fetch`（初回取得）

- **挙動**: 既定は GitHub の **リポジトリ `tar.gz`**（`getDefaultEventkitFetchUrlFromEnv`）を `fetch`（GET 全文）し、`/usr/bin/tar` で展開して **`native/eventkit-bridge` の中身のみ** `defaultCalenditDataDir()` 直下の `fetched-eventkit-bridge/` へ。`CALENDIT_EVENTKIT_FETCH_URL` / `REF` 対応。ホスト allowlist: `github.com` 等。
- **確認 UI**: 説明＋`HEAD` の `Content-Length` があるとき **MiB 目安**＋展開先。enquirer 確認。続けて** Swift で .app 作成**するか 2 つ目（`--build` または非対話 `--yes` でスキップ）。
- **解決**: `resolveEventkitBridgeRepoPath` が **fetched** ディレクトリを辿る（`CALENDIT_CONFIG_DIR` と整合した `defaultCalenditDataDir`）。
- **新規** `src/core/eventkitBridgeFetch.ts`。
